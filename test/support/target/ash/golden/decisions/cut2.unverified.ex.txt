# Generated by bubble_ex from a Bubble app's data model. Deterministic:
# the same model and name map give the same file.
#
# One file holds many modules; split them one per file in your project
# (lib/my_app/task.ex, ...). The repo is referenced but not generated:
#   mix igniter.new my_app --install ash,ash_postgres
# then add these modules and run `mix ash.codegen --dev` while iterating.
#
# The indexes need PostgreSQL extensions: list them in the repo's
#   def installed_extensions, do: ["pg_trgm"]

defmodule MyApp.Types.GeographicAddress do
  @moduledoc "A Bubble geographic address."

  use Ash.TypedStruct

  typed_struct do
    field :formatted_address, :string,
      allow_nil?: true,
      constraints: [trim?: false, allow_empty?: true]

    field :lat, :float, allow_nil?: true
    field :lng, :float, allow_nil?: true
  end
end

defmodule MyApp.Privacy.KeyedRead do
  @moduledoc """
  Policy check: an authorized read (or aggregate) of the primary `:read`
  must select records by primary key (`Ash.get`) or load them through a
  relationship; direct view in Bubble reaches a record through a
  reference, never by listing. Otherwise it is forbidden. Enumerate
  and count with `:search`.
  Generated by bubble_ex (WTF-356); NOT VERIFIED AGAINST BUBBLE.
  """
  use Ash.Policy.SimpleCheck

  alias Ash.Query.{BooleanExpression, Ref}
  alias Ash.Query.Operator.{Eq, In}

  @impl true
  def describe(_opts), do: "the read selects records by primary key"

  # A policy check, not a preparation: it also sees aggregate queries
  # (count, exists, max, ...), which run no read hooks. A
  # relationship load (`accessing_from`) is keyed by the source
  # records' IDs, possibly as `id == parent(...)`.
  @impl true
  def match?(_actor, %{query: %Ash.Query{} = query}, _opts) do
    query.context[:accessing_from] != nil or
      keyed?(query.filter && query.filter.expression, query.resource)
  end

  def match?(_actor, _context, _opts), do: false

  defp keyed?(%BooleanExpression{op: :and, left: left, right: right}, resource),
    do: keyed?(left, resource) or keyed?(right, resource)

  defp keyed?(%Eq{left: %Ref{relationship_path: [], attribute: a}, right: v}, resource),
    do: key?(a, resource) and not Ash.Expr.expr?(v)

  defp keyed?(%Eq{left: v, right: %Ref{relationship_path: [], attribute: a}}, resource),
    do: key?(a, resource) and not Ash.Expr.expr?(v)

  defp keyed?(%In{left: %Ref{relationship_path: [], attribute: a}, right: v}, resource),
    do: key?(a, resource) and not Ash.Expr.expr?(v)

  defp keyed?(_expression, _resource), do: false

  defp key?(attribute, resource),
    do: [name(attribute)] == Ash.Resource.Info.primary_key(resource)

  defp name(%{name: name}), do: name
  defp name(name), do: name
end

defmodule MyApp.Board do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "board"
    repo MyApp.Repo

    references do
      reference :creator, ignore?: true
      reference :creator_for_privacy, ignore?: true
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :location, MyApp.Types.GeographicAddress,
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :name, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :watchers, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
      sortable? false
      filter expr(parent(privacy_rule_watcher))
    end

    has_many :cards, MyApp.Card do
      source_attribute :id
      destination_attribute :board_id
      public? true
      sortable? false
      filter expr(parent(privacy_rule_busy or privacy_rule_watcher))
    end

    belongs_to :creator_for_privacy, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? false
    end

    has_many :cards_for_privacy, MyApp.Card do
      source_attribute :id
      destination_attribute :board_id
      public? false
    end
  end

  calculations do
    calculate :watcher_count, :integer, expr(length(watchers || [])),
      public?: true,
      description:
        "The length of watchers (owner decision finding:denormalized_field:d8c5d54da46b523e); not stored"

    calculate :privacy_rule_watcher, :boolean, expr(^actor(:id) in watchers),
      public?: false,
      description: "Bubble privacy rule \"Watcher\" (watcher_): its condition holds"

    calculate :privacy_rule_busy, :boolean, expr(exists(cards_for_privacy, true)),
      public?: false,
      description: "Bubble privacy rule \"Busy\" (busy_): its condition holds"
  end

  aggregates do
    count :card_count, [:cards_for_privacy],
      public?: true,
      authorize?: false,
      description:
        "The count of cards (owner decision finding:denormalized_field:805069ce233b7b93); not stored"
  end

  actions do
    defaults [:destroy, {:create, :*}, {:update, :*}]

    read :read do
      description "Direct view: records reached by primary key (Ash.get, relationship loads); an authorized read or aggregate that does not select by primary key is forbidden"
      primary? true
    end

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount. Enumerate records only through :search; :read
  # returns records by primary key. Field policies do not guard code: a
  # filter, sort or calculation written in code (not *_input) that reads a
  # field the actor may not view still sees its value, so lowered searches
  # must not reference such fields directly, nor the private *_for_privacy
  # relationships. Aggregates (count, min, max, sum, ...) over a field are
  # not covered by field policies either: never aggregate a field the actor
  # may not view.
  policies do
    policy action(:read) do
      description "Direct view reaches records by primary key or through a relationship, never by listing"
      authorize_if MyApp.Privacy.KeyedRead
    end

    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if always()
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if always()
    end
  end

  field_policies do
    private_fields :hide

    field_policy [:created_date, :modified_date, :creator_id, :slug, :location, :watchers] do
      authorize_if expr(privacy_rule_watcher)
    end

    field_policy [:name, :card_count] do
      authorize_if always()
    end

    field_policy [:watcher_count] do
      authorize_if expr(privacy_rule_busy)
      authorize_if expr(privacy_rule_watcher)
    end
  end
end

defmodule MyApp.Card do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "card"
    repo MyApp.Repo

    references do
      reference :creator, ignore?: true
      reference :assignee, ignore?: true
      reference :board, ignore?: true
      reference :creator_for_privacy, ignore?: true
      reference :assignee_for_privacy, ignore?: true
      reference :board_for_privacy, ignore?: true
    end

    custom_indexes do
      index [:points], name: "card_points_index"
      index [:status, :title], name: "card_status_title_index"
      index [:tags], name: "card_tags_gin_index", using: "gin"

      index ["to_tsvector('simple'::regconfig, coalesce(\"title\", ''))"],
        name: "card_title_fts_index",
        using: "gin"

      index ["\"title\" gin_trgm_ops"], name: "card_title_trgm_index", using: "gin"
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :assignee_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :blockers, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :board_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :points, :float, allow_nil?: true, writable?: true, public?: true

    attribute :status, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :tags, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :title, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
      sortable? false
      filter expr(parent(privacy_rule_creator))
    end

    belongs_to :assignee, MyApp.User do
      source_attribute :assignee_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
      sortable? false
      filter expr(parent(privacy_rule_creator))
    end

    belongs_to :board, MyApp.Board do
      source_attribute :board_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
      sortable? false
      filter expr(parent(privacy_rule_creator))
    end

    belongs_to :creator_for_privacy, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? false
    end

    belongs_to :assignee_for_privacy, MyApp.User do
      source_attribute :assignee_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? false
    end

    belongs_to :board_for_privacy, MyApp.Board do
      source_attribute :board_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? false
    end
  end

  calculations do
    calculate :board_watcher_count, :integer, expr(length(board_for_privacy.watchers || [])),
      public?: true,
      description:
        "The length of board.watchers (owner decision finding:denormalized_field:671c6b64fcee5682); not stored"

    calculate :privacy_rule_creator, :boolean, expr(creator_id == ^actor(:id)),
      public?: false,
      description: "Bubble privacy rule \"Creator\" (creator_): its condition holds"
  end

  aggregates do
    count :board_card_count, [:board_for_privacy, :cards_for_privacy],
      public?: true,
      authorize?: false,
      description:
        "The count of board.cards (owner decision finding:denormalized_field:936d3a0bb3475c6b); not stored"
  end

  actions do
    defaults [:destroy, {:create, :*}, {:update, :*}]

    read :read do
      description "Direct view: records reached by primary key (Ash.get, relationship loads); an authorized read or aggregate that does not select by primary key is forbidden"
      primary? true
    end

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount. Enumerate records only through :search; :read
  # returns records by primary key. Field policies do not guard code: a
  # filter, sort or calculation written in code (not *_input) that reads a
  # field the actor may not view still sees its value, so lowered searches
  # must not reference such fields directly, nor the private *_for_privacy
  # relationships. Aggregates (count, min, max, sum, ...) over a field are
  # not covered by field policies either: never aggregate a field the actor
  # may not view.
  policies do
    policy action(:read) do
      description "Direct view reaches records by primary key or through a relationship, never by listing"
      authorize_if MyApp.Privacy.KeyedRead
    end

    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if expr(privacy_rule_creator)
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if expr(privacy_rule_creator)
    end
  end

  field_policies do
    private_fields :hide

    field_policy [
      :created_date,
      :modified_date,
      :creator_id,
      :slug,
      :assignee_id,
      :blockers,
      :board_id,
      :points,
      :status,
      :tags,
      :title,
      :board_watcher_count,
      :board_card_count
    ] do
      authorize_if expr(privacy_rule_creator)
    end
  end
end

defmodule MyApp.User do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "user"
    repo MyApp.Repo

    references do
      reference :creator, ignore?: true
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :email, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :name, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
      sortable? false
    end
  end

  actions do
    defaults [:destroy, {:create, :*}, {:update, :*}]

    read :read do
      description "Direct view: records reached by primary key (Ash.get, relationship loads); an authorized read or aggregate that does not select by primary key is forbidden"
      primary? true
    end

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount. Enumerate records only through :search; :read
  # returns records by primary key. Field policies do not guard code: a
  # filter, sort or calculation written in code (not *_input) that reads a
  # field the actor may not view still sees its value, so lowered searches
  # must not reference such fields directly, nor the private *_for_privacy
  # relationships. Aggregates (count, min, max, sum, ...) over a field are
  # not covered by field policies either: never aggregate a field the actor
  # may not view.
  policies do
    policy action(:read) do
      description "Direct view reaches records by primary key or through a relationship, never by listing"
      authorize_if MyApp.Privacy.KeyedRead
    end

    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if always()
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if always()
    end
  end

  field_policies do
    private_fields :hide

    field_policy [:created_date, :modified_date, :creator_id, :slug, :email, :name] do
      authorize_if always()
    end
  end
end

defmodule MyApp do
  use Ash.Domain

  resources do
    resource MyApp.Board
    resource MyApp.Card
    resource MyApp.User
  end
end

defmodule MyApp.Privacy do
  @moduledoc """
  Privacy rules compiled from Bubble to Ash policies by bubble_ex (WTF-356).

  NOT VERIFIED AGAINST BUBBLE. The policies rest on Bubble semantics not
  yet confirmed by replaying the app (WTF-384/385): do not ship them to
  users until they are. See `verified?/0`.
  """

  @doc "Whether the generated policies are verified against Bubble: not yet."
  def verified?, do: false

  @doc "The relationships of the actor that the policies read."
  def actor_loads, do: []

  @doc "The resource of the actor (Bubble's User)."
  def actor_resource, do: MyApp.User

  @doc """
  Reads the actor (a user or a user ID) afresh with `actor_loads/0`,
  bypassing authorization: the policies must see its current values.
  Call it on every request and LiveView mount, never reuse a stored
  actor. Returns nil (logged out) for nil or an unknown ID.
  """
  def load_actor(nil), do: nil
  def load_actor(%{id: id}), do: load_actor(id)

  def load_actor(id) when is_binary(id) do
    case Ash.get(MyApp.User, id, load: actor_loads(), authorize?: false) do
      {:ok, actor} -> actor
      {:error, _} -> nil
    end
  end
end
