# Generated by bubble_ex from a Bubble app's data model. Deterministic:
# the same model and name map give the same file.
#
# One file holds many modules; split them one per file in your project
# (lib/my_app/task.ex, ...). The repo is referenced but not generated:
#   mix igniter.new my_app --install ash,ash_postgres
# then add these modules and run `mix ash.codegen --dev` while iterating.

defmodule MyApp.Enums.Status do
  use Ash.Type.Enum, values: [{"open", [label: "Open"]}, {"closed", [label: "Closed"]}]

  @attributes %{"open" => %{color: "#00aa00"}, "closed" => %{color: "#aa0000"}}

  @doc "The attribute values of `value`."
  def attributes(value), do: Map.fetch!(@attributes, value)
end

defmodule MyApp.Types.DateRange do
  @moduledoc "A Bubble date range. Whether each end is inclusive is not verified against Bubble."

  use Ash.TypedStruct

  typed_struct do
    field :start, :utc_datetime_usec, allow_nil?: true
    field :end, :utc_datetime_usec, allow_nil?: true
  end
end

defmodule MyApp.Types.GeographicAddress do
  @moduledoc "A Bubble geographic address."

  use Ash.TypedStruct

  typed_struct do
    field :formatted_address, :string,
      allow_nil?: true,
      constraints: [trim?: false, allow_empty?: true]

    field :lat, :float, allow_nil?: true
    field :lng, :float, allow_nil?: true
  end
end

defmodule MyApp.Types.NumberRange do
  @moduledoc "A Bubble number range. Whether each end is inclusive is not verified against Bubble."

  use Ash.TypedStruct

  typed_struct do
    field :min, :float, allow_nil?: true
    field :max, :float, allow_nil?: true
  end
end

defmodule MyApp.Project do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "project"
    repo MyApp.Repo

    references do
      reference :creator, ignore?: true
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :name, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
    end
  end

  actions do
    defaults [:read, :destroy, {:create, :*}, {:update, :*}]

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount.
  policies do
    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if always()
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if always()
    end
  end

  field_policies do
    field_policy [:created_date, :modified_date, :creator_id, :slug, :name] do
      authorize_if always()
    end
  end
end

defmodule MyApp.Task do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "task"
    repo MyApp.Repo

    migration_types dates: {:array, :utc_datetime_usec}

    references do
      reference :creator, ignore?: true
      reference :owner, ignore?: true
      reference :project, ignore?: true
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :attachment, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :budget, MyApp.Types.NumberRange, allow_nil?: true, writable?: true, public?: true
    attribute :checks, {:array, :boolean}, allow_nil?: true, writable?: true, public?: true

    attribute :cover, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :dates, {:array, :utc_datetime_usec},
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :done, :boolean, allow_nil?: true, writable?: true, public?: true, default: false
    attribute :due, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :duration, :float, allow_nil?: true, writable?: true, public?: true
    attribute :estimate, :float, allow_nil?: true, writable?: true, public?: true, default: 1.0

    attribute :files, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :images, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :labels, {:array, MyApp.Enums.Status},
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :notes, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :owner_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :place, MyApp.Types.GeographicAddress,
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :places, {:array, MyApp.Types.GeographicAddress},
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :project_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :ranges, {:array, MyApp.Types.DateRange},
      allow_nil?: true,
      writable?: true,
      public?: true

    attribute :scores, {:array, :float}, allow_nil?: true, writable?: true, public?: true
    attribute :status, MyApp.Enums.Status, allow_nil?: true, writable?: true, public?: true

    attribute :subtasks, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :title, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      default: "Untitled",
      constraints: [trim?: false, allow_empty?: true]

    attribute :watchers, {:array, :string},
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [items: [trim?: false, allow_empty?: true]]

    attribute :window, MyApp.Types.DateRange, allow_nil?: true, writable?: true, public?: true
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
    end

    belongs_to :owner, MyApp.User do
      source_attribute :owner_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
    end

    belongs_to :project, MyApp.Project do
      source_attribute :project_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
    end
  end

  actions do
    defaults [:read, :destroy, {:create, :*}, {:update, :*}]

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount.
  policies do
    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if always()
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if always()
    end
  end

  field_policies do
    field_policy [
      :created_date,
      :modified_date,
      :creator_id,
      :slug,
      :attachment,
      :budget,
      :checks,
      :cover,
      :dates,
      :done,
      :due,
      :duration,
      :estimate,
      :files,
      :images,
      :labels,
      :notes,
      :owner_id,
      :place,
      :places,
      :project_id,
      :ranges,
      :scores,
      :status,
      :subtasks,
      :title,
      :watchers,
      :window
    ] do
      authorize_if always()
    end
  end
end

defmodule MyApp.User do
  use Ash.Resource,
    domain: MyApp,
    data_layer: AshPostgres.DataLayer,
    authorizers: [Ash.Policy.Authorizer]

  postgres do
    table "user"
    repo MyApp.Repo

    references do
      reference :creator, ignore?: true
    end
  end

  attributes do
    attribute :id, :string,
      primary_key?: true,
      allow_nil?: false,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :created_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true
    attribute :modified_date, :utc_datetime_usec, allow_nil?: true, writable?: true, public?: true

    attribute :creator_id, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :slug, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :email, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]

    attribute :nickname, :string,
      allow_nil?: true,
      writable?: true,
      public?: true,
      constraints: [trim?: false, allow_empty?: true]
  end

  relationships do
    belongs_to :creator, MyApp.User do
      source_attribute :creator_id
      destination_attribute :id
      attribute_type :string
      define_attribute? false
      allow_nil? true
      public? true
    end
  end

  actions do
    defaults [:read, :destroy, {:create, :*}, {:update, :*}]

    read :search do
      description "Bubble \"Do a search for\": the records the user may find in searches"
    end
  end

  # Privacy rules compiled from Bubble by bubble_ex (WTF-356).
  # NOT VERIFIED AGAINST BUBBLE: do not ship these policies to users before
  # the replay verification (WTF-384/385) confirms the semantics they rest on.
  # Load the actor with the Privacy module's load_actor/1 on every request
  # and LiveView mount.
  policies do
    policy action(:read) do
      description "Direct view (by ID, or through a reference): the user may view some field of the record"
      authorize_if always()
    end

    policy action(:search) do
      description "Searches: the user may find the record in searches"
      authorize_if always()
    end
  end

  field_policies do
    field_policy [:created_date, :modified_date, :creator_id, :slug, :email, :nickname] do
      authorize_if always()
    end
  end
end

defmodule MyApp do
  use Ash.Domain

  resources do
    resource MyApp.Project
    resource MyApp.Task
    resource MyApp.User
  end
end

defmodule MyApp.Privacy do
  @moduledoc """
  Privacy rules compiled from Bubble to Ash policies by bubble_ex (WTF-356).

  NOT VERIFIED AGAINST BUBBLE. The policies rest on Bubble semantics not
  yet confirmed by replaying the app (WTF-384/385): do not ship them to
  users until they are. See `verified?/0`.
  """

  @doc "Whether the generated policies are verified against Bubble: not yet."
  def verified?, do: false

  @doc "The relationships of the actor that the policies read."
  def actor_loads, do: []

  @doc "The resource of the actor (Bubble's User)."
  def actor_resource, do: MyApp.User

  @doc """
  Reads the actor (a user or a user ID) afresh with `actor_loads/0`,
  bypassing authorization: the policies must see its current values.
  Call it on every request and LiveView mount, never reuse a stored
  actor. Returns nil (logged out) for nil or an unknown ID.
  """
  def load_actor(nil), do: nil
  def load_actor(%{id: id}), do: load_actor(id)

  def load_actor(id) when is_binary(id) do
    case Ash.get(MyApp.User, id, load: actor_loads(), authorize?: false) do
      {:ok, actor} -> actor
      {:error, _} -> nil
    end
  end
end
