# schema module
defmodule MyApp.Channel do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:display, :string, autogenerate: false}
  @foreign_key_type :string

  schema "channel" do
    field :db_value, :string
    field :sort_factor, :float
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:db_value, :sort_factor]) |> validate_required([:display])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateChannel do
  use Ecto.Migration

  def change do
    create table("channel", primary_key: false) do
      add :display, :string, primary_key: true
      add :db_value, :string
      add :sort_factor, :float
    end
  end
end

# schema module
defmodule MyApp.Legacy do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:display, :string, autogenerate: false}
  @foreign_key_type :string

  schema "legacy" do

  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, []) |> validate_required([:display])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateLegacy do
  use Ecto.Migration

  def change do
    create table("legacy", primary_key: false) do
      add :display, :string, primary_key: true
    end
  end
end

# schema module
defmodule MyApp.Orphan do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:display, :string, autogenerate: false}
  @foreign_key_type :string

  schema "orphan" do

  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, []) |> validate_required([:display])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateOrphan do
  use Ecto.Migration

  def change do
    create table("orphan", primary_key: false) do
      add :display, :string, primary_key: true
    end
  end
end

# schema module
defmodule MyApp.Priority do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:display, :string, autogenerate: false}
  @foreign_key_type :string

  schema "priority" do
    field :channels, :string
    field :color, :string
    field :db_value, :string
    field :deleted, :string
    field :escalates_to, :string
    field :icon, :string
    field :sort_factor, :float
    field :tooltip, :string
    field :urgent, :string
    field :weight, :float
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:channels, :color, :db_value, :deleted, :escalates_to, :icon, :sort_factor, :tooltip, :urgent, :weight]) |> validate_required([:display])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreatePriority do
  use Ecto.Migration

  def change do
    create table("priority", primary_key: false) do
      add :display, :string, primary_key: true
      add :channels, :string
      add :color, :string
      add :db_value, :string
      add :deleted, :string
      add :escalates_to, :string
      add :icon, :string
      add :sort_factor, :float
      add :tooltip, :string
      add :urgent, :string
      add :weight, :float
    end
  end
end

# schema module
defmodule MyApp.Ticket do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "ticket" do
    field :channels, {:array, :string}
    belongs_to :priority, MyApp.Priority, foreign_key: :priority_id, references: :display, type: :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:channels, :priority_id]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateTicket do
  use Ecto.Migration

  def change do
    create table("ticket", primary_key: false) do
      add :_id, :string, primary_key: true
      add :channels, {:array, :string}
      add :priority_id, :string
    end
    create index("ticket", [:priority_id])
  end
end
--- diagnostics ---
