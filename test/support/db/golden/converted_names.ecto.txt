# schema module
defmodule MyApp.BlogPost do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "blog_post" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateBlogPost do
  use Ecto.Migration

  def change do
    create table("blog_post", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
    end
    create index("blog_post", [:created_by_id])
  end
end

# schema module
defmodule MyApp.Post do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "post" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :created_date_2, :utc_datetime_usec
    belongs_to :created_by_2, MyApp.User, foreign_key: :created_by_2_id, references: :_id, type: :string
    field :created_by_id_2, :string
    field :foo_bar, :string
    field :foo_bar_2, :float
    field :owner_id, :string
    belongs_to :owner_2, MyApp.User, foreign_key: :owner_2_id, references: :_id, type: :string
    field :bubble_id, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :created_date_2, :created_by_2_id, :created_by_id_2, :foo_bar, :foo_bar_2, :owner_id, :owner_2_id, :bubble_id]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreatePost do
  use Ecto.Migration

  def change do
    create table("post", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :created_date_2, :utc_datetime_usec
      add :created_by_2_id, :string
      add :created_by_id_2, :string
      add :foo_bar, :string
      add :foo_bar_2, :float
      add :owner_id, :string
      add :owner_2_id, :string
      add :bubble_id, :string
    end
    create index("post", [:created_by_id])
    create index("post", [:created_by_2_id])
    create index("post", [:owner_2_id])
  end
end

# schema module
defmodule MyApp.Repo2 do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "repo_2" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateRepo2 do
  use Ecto.Migration

  def change do
    create table("repo_2", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
    end
    create index("repo_2", [:created_by_id])
  end
end

# schema module
defmodule MyApp.User do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "user" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :email, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :email]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateUser do
  use Ecto.Migration

  def change do
    create table("user", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :email, :string
    end
    create index("user", [:created_by_id])
  end
end

# schema module
defmodule MyApp.BlogPost2 do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:db_value, :string, autogenerate: false}
  @foreign_key_type :string

  schema "blog_post_2" do
    field :display, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:display]) |> validate_required([:db_value])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateBlogPost2 do
  use Ecto.Migration

  def change do
    create table("blog_post_2", primary_key: false) do
      add :db_value, :string, primary_key: true
      add :display, :string
    end
  end
end
--- diagnostics ---
db_converted_name_suffixed /option_sets/blog_post_os
db_converted_name_suffixed /user_types/post/fields/a_created_date_date/value
db_converted_name_suffixed /user_types/post/fields/b_created_by_user/value
db_converted_name_suffixed /user_types/post/fields/c_created_by_id_text/value
db_converted_name_suffixed /user_types/post/fields/e_foo_bar_number/value
db_converted_name_suffixed /user_types/post/fields/g_owner_user/value
db_converted_name_suffixed /user_types/repo
model_synthesized_user_type /user_types/user
