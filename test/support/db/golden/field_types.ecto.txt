# schema module
defmodule MyApp.Project do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "project" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :name, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :name]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateProject do
  use Ecto.Migration

  def change do
    create table("project", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :name, :string
    end
    create index("project", [:created_by_id], name: "project_created_by_id_index")
  end
end

# schema module
defmodule MyApp.Task do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "task" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :attachment, :string
    field :budget, :map
    field :checks, {:array, :boolean}
    field :cover, :string
    field :dates, {:array, :utc_datetime_usec}
    field :done, :boolean
    field :due, :utc_datetime_usec
    field :duration, :map
    field :estimate, :float
    field :files, {:array, :string}
    field :images, {:array, :string}
    field :labels, {:array, :string}
    field :notes, {:array, :string}
    belongs_to :owner, MyApp.User, foreign_key: :owner_id, references: :_id, type: :string
    field :place, :map
    field :places, {:array, :map}
    belongs_to :project, MyApp.Project, foreign_key: :project_id, references: :_id, type: :string
    field :ranges, {:array, :map}
    field :scores, {:array, :float}
    belongs_to :status, MyApp.Status, foreign_key: :status_id, references: :db_value, type: :string
    field :subtasks, {:array, :string}
    field :title, :string
    field :watchers, {:array, :string}
    field :window, :map
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :attachment, :budget, :checks, :cover, :dates, :done, :due, :duration, :estimate, :files, :images, :labels, :notes, :owner_id, :place, :places, :project_id, :ranges, :scores, :status_id, :subtasks, :title, :watchers, :window]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateTask do
  use Ecto.Migration

  def change do
    create table("task", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :attachment, :string
      add :budget, :map
      add :checks, {:array, :boolean}
      add :cover, :string
      add :dates, {:array, :utc_datetime_usec}
      add :done, :boolean
      add :due, :utc_datetime_usec
      add :duration, :map
      add :estimate, :float
      add :files, {:array, :string}
      add :images, {:array, :string}
      add :labels, {:array, :string}
      add :notes, {:array, :string}
      add :owner_id, :string
      add :place, :map
      add :places, {:array, :map}
      add :project_id, :string
      add :ranges, {:array, :map}
      add :scores, {:array, :float}
      add :status_id, :string
      add :subtasks, {:array, :string}
      add :title, :string
      add :watchers, {:array, :string}
      add :window, :map
    end
    create index("task", [:created_by_id], name: "task_created_by_id_index")
    create index("task", [:owner_id], name: "task_owner_id_index")
    create index("task", [:project_id], name: "task_project_id_index")
    create index("task", [:status_id], name: "task_status_id_index")
  end
end

# schema module
defmodule MyApp.User do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "user" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :email, :string
    field :nickname, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :email, :nickname]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateUser do
  use Ecto.Migration

  def change do
    create table("user", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :email, :string
      add :nickname, :string
    end
    create index("user", [:created_by_id], name: "user_created_by_id_index")
  end
end

# schema module
defmodule MyApp.Status do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:db_value, :string, autogenerate: false}
  @foreign_key_type :string

  schema "status" do
    field :display, :string
    field :color, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:display, :color]) |> validate_required([:db_value])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateStatus do
  use Ecto.Migration

  def change do
    create table("status", primary_key: false) do
      add :db_value, :string, primary_key: true
      add :display, :string
      add :color, :string
    end
  end
end
--- diagnostics ---
