-- SQLite DDL for Bubble app
-- NOTE: arrays stored as JSON text.
-- NOTE: Bubble groups (custom/option/api) have no SQLite schema equivalent; encoded as a table-name prefix.

CREATE TABLE IF NOT EXISTS "custom__Order" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "Buyer" TEXT,
  "Items" TEXT,  -- list<TEXT>; store as JSON
  "Kind" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__User" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "email" TEXT,
  "Name" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "option__Kind" (
  "db_value" TEXT NOT NULL,
  "Display" TEXT,
  "Code" REAL,
  PRIMARY KEY ("db_value")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom__Order"."Created By" -> "custom__User"."_id"
-- "custom__Order"."Buyer" -> "custom__User"."_id"
-- "custom__Order"."Kind" -> "option__Kind"."db_value"
-- "custom__User"."Created By" -> "custom__User"."_id"
--- diagnostics ---
db_reference_to_omitted /user_types/order/%f3/items_list_custom_item/%v
