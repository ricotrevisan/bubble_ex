CREATE SCHEMA [custom];
GO

CREATE TABLE [custom].[Draft] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  CONSTRAINT [PK_draft] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[Note] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [Body] NVARCHAR(MAX),
  [Secret] NVARCHAR(MAX),
  CONSTRAINT [PK_note] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[Public page] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [Slug text] NVARCHAR(MAX),
  CONSTRAINT [PK_public_page] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[User] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [email] NVARCHAR(MAX),
  [Admin] BIT,
  CONSTRAINT [PK_user] PRIMARY KEY ([_id])
);

ALTER TABLE [custom].[Draft]
  ADD CONSTRAINT [FK_draft_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[Note]
  ADD CONSTRAINT [FK_note_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[Public page]
  ADD CONSTRAINT [FK_public_page_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[User]
  ADD CONSTRAINT [FK_user_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
--- diagnostics ---
