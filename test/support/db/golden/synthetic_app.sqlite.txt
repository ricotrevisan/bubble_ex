-- SQLite DDL for Bubble app
-- NOTE: arrays stored as JSON text.
-- NOTE: Bubble groups (custom/option/api) have no SQLite schema equivalent; encoded as a table-name prefix.

CREATE TABLE IF NOT EXISTS "custom__Onboarding Answer" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "label" TEXT,
  "score" REAL,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__Survey Response" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "answer" TEXT,
  "onboarding answer" TEXT,
  "rating" REAL,
  "status" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__User" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "email" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "option__Status Type" (
  "db_value" TEXT NOT NULL,
  "Display" TEXT,
  "Color" TEXT,
  PRIMARY KEY ("db_value")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom__Onboarding Answer"."Created By" -> "custom__User"."_id"
-- "custom__Survey Response"."Created By" -> "custom__User"."_id"
-- "custom__Survey Response"."onboarding answer" -> "custom__Onboarding Answer"."_id"
-- "custom__Survey Response"."status" -> "option__Status Type"."db_value"
-- "custom__User"."Created By" -> "custom__User"."_id"
--- diagnostics ---
model_synthesized_user_type /user_types/user
