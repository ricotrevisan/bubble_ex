-- SQLite DDL for Bubble app
-- NOTE: arrays stored as JSON text; FKs require PRAGMA foreign_keys = ON.
-- NOTE: Bubble groups (custom/option/api) have no SQLite schema equivalent; encoded as a table-name prefix.

PRAGMA foreign_keys = ON;

CREATE TABLE IF NOT EXISTS "custom__00. Thing - Join" (
  "_id" TEXT NOT NULL,
  "11. Done" INTEGER,
  "40. Sort: Thing Title" TEXT,
  "__struct__" TEXT,
  "id" TEXT,
  "inserted_at" TEXT,
  "Title" TEXT,
  "Title_2" TEXT,
  "type" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__🚀 Launch" (
  "_id" TEXT NOT NULL,
  "🔥" TEXT,
  "Name ✨" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__Thing" (
  "_id" TEXT NOT NULL,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__Thing_2" (
  "_id" TEXT NOT NULL,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__User" (
  "_id" TEXT NOT NULL,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "option__40. Levels" (
  "db_value" TEXT NOT NULL,
  "Display" TEXT,
  PRIMARY KEY ("db_value")
);
--- diagnostics ---
db_name_suffixed /user_types/00__thing___join/fields/title_text1
db_name_suffixed /user_types/thing0
model_synthesized_user_type /user_types/user
