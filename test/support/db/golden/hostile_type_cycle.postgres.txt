CREATE SCHEMA IF NOT EXISTS "custom";

CREATE TABLE "custom"."A" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "B" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."B" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "C" text[],
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."C" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "A" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."User" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "email" text,
  PRIMARY KEY ("_id")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom"."A"."Created By" -> "custom"."User"."_id"
-- "custom"."A"."B" -> "custom"."B"."_id"
-- "custom"."B"."Created By" -> "custom"."User"."_id"
-- "custom"."C"."Created By" -> "custom"."User"."_id"
-- "custom"."C"."A" -> "custom"."A"."_id"
-- "custom"."User"."Created By" -> "custom"."User"."_id"

COMMENT ON COLUMN "custom"."A"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."A"."B" IS E'References "custom"."B"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."B"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."C"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."C"."A" IS E'References "custom"."A"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."User"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
--- diagnostics ---
model_synthesized_user_type /user_types/user
