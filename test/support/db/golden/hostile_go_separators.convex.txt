// convex/schema.ts
// Generated by BubbleEx.
// NOTE: :reference columns -> v.string() (Bubble text ids, not Convex document ids; re-key after data migration).
// NOTE: :enum columns -> v.string() (option-set member values are not rendered).
// NOTE: :api group tables are omitted.
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  batchGOSplit: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    aGOB: v.string(),
    cGoD: v.string(),
    eGOF: v.string(),
    gGO5H: v.string(),
    iGOJ: v.string(),
    aGOB2: v.string(),
    kREvilSqlDelXGO: v.string(),
    ownerGO: v.string(), // reference -> re-key to v.id(...)
    kind: v.string(), // enum -> option set (member values not rendered)
  }),

  user: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    email: v.string(),
  }),

  kindGO: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
  }),
});
--- diagnostics ---
model_option_key_missing /option_sets/kind/values/one
db_converted_name_suffixed /user_types/batch/fields/f_spaced_text/value
