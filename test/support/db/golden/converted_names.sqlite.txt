-- SQLite DDL for Bubble app
-- NOTE: arrays stored as JSON text.
-- NOTE: Bubble groups (custom/option/api) have no SQLite schema equivalent; encoded as a table-name prefix.

CREATE TABLE IF NOT EXISTS "custom__Blog Post" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__Post" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "created_date" TEXT,
  "Created-By" TEXT,
  "created_by_id" TEXT,
  "Foo Bar" TEXT,
  "foo-bar" REAL,
  "Owner ID" TEXT,
  "Owner" TEXT,
  "bubble_id" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__Repo" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__User" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "email" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "option__Blog-Post" (
  "db_value" TEXT NOT NULL,
  "Display" TEXT,
  PRIMARY KEY ("db_value")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom__Blog Post"."Created By" -> "custom__User"."_id"
-- "custom__Post"."Created By" -> "custom__User"."_id"
-- "custom__Post"."Created-By" -> "custom__User"."_id"
-- "custom__Post"."Owner" -> "custom__User"."_id"
-- "custom__Repo"."Created By" -> "custom__User"."_id"
-- "custom__User"."Created By" -> "custom__User"."_id"
--- diagnostics ---
model_synthesized_user_type /user_types/user
