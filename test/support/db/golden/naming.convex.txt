// convex/schema.ts
// Generated by BubbleEx.
// NOTE: :reference columns -> v.string() (Bubble text ids, not Convex document ids; re-key after data migration).
// NOTE: :enum columns -> v.string() (option-set member values are not rendered).
// NOTE: :api group tables are omitted.
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  field00ThingJoin: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    field11Done: v.boolean(),
    field40SortThingTitle: v.string(),
    struct: v.string(),
    id: v.string(),
    insertedAt: v.float64(),
    title: v.string(),
    title2: v.string(),
    type: v.string(),
  }),

  launch: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    field: v.string(),
    name: v.string(),
  }),

  thing: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
  }),

  thing2: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
  }),

  user: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
  }),

  field40Levels: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
  }),
});
--- diagnostics ---
db_name_suffixed /user_types/00__thing___join/fields/title_text1
db_name_suffixed /user_types/thing0
model_synthesized_user_type /user_types/user
