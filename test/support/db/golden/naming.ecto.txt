# schema module
defmodule MyApp.N00ThingJoin do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "n00_thing_join" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :n11_done, :boolean
    field :n40_sort_thing_title, :string
    field :_struct, :string
    field :id, :string
    field :inserted_at, :utc_datetime_usec
    field :title, :string
    field :title_2, :string
    field :type, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :n11_done, :n40_sort_thing_title, :_struct, :id, :inserted_at, :title, :title_2, :type]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateN00ThingJoin do
  use Ecto.Migration

  def change do
    create table("n00_thing_join", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :n11_done, :boolean
      add :n40_sort_thing_title, :string
      add :_struct, :string
      add :id, :string
      add :inserted_at, :utc_datetime_usec
      add :title, :string
      add :title_2, :string
      add :type, :string
    end
    create index("n00_thing_join", [:created_by_id], name: "n00_thing_join_created_by_id_index")
  end
end

# schema module
defmodule MyApp.Launch do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "launch" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :field, :string
    field :name, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :field, :name]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateLaunch do
  use Ecto.Migration

  def change do
    create table("launch", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :field, :string
      add :name, :string
    end
    create index("launch", [:created_by_id], name: "launch_created_by_id_index")
  end
end

# schema module
defmodule MyApp.Thing do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "thing" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateThing do
  use Ecto.Migration

  def change do
    create table("thing", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
    end
    create index("thing", [:created_by_id], name: "thing_created_by_id_index")
  end
end

# schema module
defmodule MyApp.Thing2 do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "thing_2" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateThing2 do
  use Ecto.Migration

  def change do
    create table("thing_2", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
    end
    create index("thing_2", [:created_by_id], name: "thing_2_created_by_id_index")
  end
end

# schema module
defmodule MyApp.User do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "user" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :email, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :email]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateUser do
  use Ecto.Migration

  def change do
    create table("user", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :email, :string
    end
    create index("user", [:created_by_id], name: "user_created_by_id_index")
  end
end

# schema module
defmodule MyApp.N40Levels do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:db_value, :string, autogenerate: false}
  @foreign_key_type :string

  schema "n40_levels" do
    field :display, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:display]) |> validate_required([:db_value])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateN40Levels do
  use Ecto.Migration

  def change do
    create table("n40_levels", primary_key: false) do
      add :db_value, :string, primary_key: true
      add :display, :string
    end
  end
end
--- diagnostics ---
db_name_suffixed /user_types/00__thing___join/fields/title_text1
db_name_suffixed /user_types/thing0
model_synthesized_user_type /user_types/user
