-- SQLite DDL for Bubble app
-- NOTE: arrays stored as JSON text.
-- NOTE: Bubble groups (custom/option/api) have no SQLite schema equivalent; encoded as a table-name prefix.

CREATE TABLE IF NOT EXISTS "custom__A" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "B" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__B" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "C" TEXT,  -- list<TEXT>; store as JSON
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__C" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "A" TEXT,
  PRIMARY KEY ("_id")
);

CREATE TABLE IF NOT EXISTS "custom__User" (
  "_id" TEXT NOT NULL,
  "Created Date" TEXT,
  "Modified Date" TEXT,
  "Created By" TEXT,
  "Slug" TEXT,
  "email" TEXT,
  PRIMARY KEY ("_id")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom__A"."Created By" -> "custom__User"."_id"
-- "custom__A"."B" -> "custom__B"."_id"
-- "custom__B"."Created By" -> "custom__User"."_id"
-- "custom__C"."Created By" -> "custom__User"."_id"
-- "custom__C"."A" -> "custom__A"."_id"
-- "custom__User"."Created By" -> "custom__User"."_id"
--- diagnostics ---
model_synthesized_user_type /user_types/user
