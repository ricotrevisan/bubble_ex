CREATE SCHEMA IF NOT EXISTS "custom";
CREATE SCHEMA IF NOT EXISTS "option";

CREATE TABLE "custom"."Project" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "Name" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."Task" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "Done?" boolean,
  "Project" text,
  "Status" text,
  "Title" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."User" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "email" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "option"."Status" (
  "db_value" text,
  "Display" text,
  PRIMARY KEY ("db_value")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom"."Project"."Created By" -> "custom"."User"."_id"
-- "custom"."Task"."Created By" -> "custom"."User"."_id"
-- "custom"."Task"."Project" -> "custom"."Project"."_id"
-- "custom"."Task"."Status" -> "option"."Status"."db_value"
-- "custom"."User"."Created By" -> "custom"."User"."_id"
--- diagnostics ---
model_synthesized_user_type /user_types/user
