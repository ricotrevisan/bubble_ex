// convex/schema.ts
// Generated by BubbleEx.
// NOTE: :reference columns -> v.string() (Bubble text ids, not Convex document ids; re-key after data migration).
// NOTE: :enum columns -> v.string() (option-set member values are not rendered).
// NOTE: :api group tables are omitted.
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  ticket: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    channels: v.array(v.string()), // enum -> option set (member values not rendered)
    priority: v.string(), // enum -> option set (member values not rendered)
  }),

  user: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
  }),

  channel: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
  }),

  orphan: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
    parent: v.string(), // enum -> option set (member values not rendered)
  }),

  priority: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
    channels: v.array(v.string()), // enum -> option set (member values not rendered)
    color: v.string(),
    escalatesTo: v.string(), // enum -> option set (member values not rendered)
    icon: v.string(),
    urgent: v.boolean(),
    weight: v.float64(),
  }),
});
--- diagnostics ---
model_unresolved_target /option_sets/orphan/attributes/parent/value
db_duplicate_option_value_dropped /option_sets/priority/values/bAg
model_duplicate_option_key /option_sets/priority/values/bAg
model_option_key_missing /option_sets/priority/values/bAf
model_undeclared_option_attribute_value /option_sets/priority/values/bAh/tooltip
model_synthesized_user_type /user_types/user
