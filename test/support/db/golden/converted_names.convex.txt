// convex/schema.ts
// Generated by BubbleEx.
// NOTE: :reference columns -> v.string() (Bubble text ids, not Convex document ids; re-key after data migration).
// NOTE: :enum columns -> v.string() (option-set member values are not rendered).
// NOTE: :api group tables are omitted.
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  blogPost: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
  }),

  post: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    createdDate2: v.float64(),
    createdBy2: v.string(), // reference -> re-key to v.id(...)
    createdById: v.string(),
    fooBar: v.string(),
    fooBar2: v.float64(),
    ownerID: v.string(),
    owner: v.string(), // reference -> re-key to v.id(...)
    bubbleId2: v.string(),
  }),

  repo: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
  }),

  user: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    email: v.string(),
  }),

  blogPost2: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
  }),
});
--- diagnostics ---
db_converted_name_suffixed /option_sets/blog_post_os
db_converted_name_suffixed /user_types/post/fields/a_created_date_date/value
db_converted_name_suffixed /user_types/post/fields/b_created_by_user/value
db_converted_name_suffixed /user_types/post/fields/e_foo_bar_number/value
db_converted_name_suffixed /user_types/post/fields/h_bubble_id_text/value
model_synthesized_user_type /user_types/user
