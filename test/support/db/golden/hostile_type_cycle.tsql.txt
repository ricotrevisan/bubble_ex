CREATE SCHEMA [custom];
GO

CREATE TABLE [custom].[A] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [B] NVARCHAR(450),
  CONSTRAINT [PK_a] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[B] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [C] NVARCHAR(MAX) /* list<ref>: consider a junction table */,
  CONSTRAINT [PK_b] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[C] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [A] NVARCHAR(450),
  CONSTRAINT [PK_c] PRIMARY KEY ([_id])
);

CREATE TABLE [custom].[User] (
  [_id] NVARCHAR(450),
  [Created Date] DATETIME2,
  [Modified Date] DATETIME2,
  [Created By] NVARCHAR(450),
  [Slug] NVARCHAR(MAX),
  [email] NVARCHAR(MAX),
  CONSTRAINT [PK_user] PRIMARY KEY ([_id])
);

ALTER TABLE [custom].[A]
  ADD CONSTRAINT [FK_a_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[A]
  ADD CONSTRAINT [FK_a_b]
  FOREIGN KEY ([B])
  REFERENCES [custom].[B] ([_id]);
ALTER TABLE [custom].[B]
  ADD CONSTRAINT [FK_b_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[C]
  ADD CONSTRAINT [FK_c_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
ALTER TABLE [custom].[C]
  ADD CONSTRAINT [FK_c_a]
  FOREIGN KEY ([A])
  REFERENCES [custom].[A] ([_id]);
ALTER TABLE [custom].[User]
  ADD CONSTRAINT [FK_user_created_by]
  FOREIGN KEY ([Created By])
  REFERENCES [custom].[User] ([_id]);
--- diagnostics ---
model_synthesized_user_type /user_types/user
