# schema module
defmodule MyApp.OnboardingAnswer do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "onboarding_answer" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :label, :string
    field :score, :float
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :label, :score]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateOnboardingAnswer do
  use Ecto.Migration

  def change do
    create table("onboarding_answer", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :label, :string
      add :score, :float
    end
    create index("onboarding_answer", [:created_by_id], name: "onboarding_answer_created_by_id_index")
  end
end

# schema module
defmodule MyApp.SurveyResponse do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "survey_response" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :answer, :string
    belongs_to :onboarding_answer, MyApp.OnboardingAnswer, foreign_key: :onboarding_answer_id, references: :_id, type: :string
    field :rating, :float
    belongs_to :status, MyApp.StatusType, foreign_key: :status_id, references: :db_value, type: :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :answer, :onboarding_answer_id, :rating, :status_id]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateSurveyResponse do
  use Ecto.Migration

  def change do
    create table("survey_response", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :answer, :string
      add :onboarding_answer_id, :string
      add :rating, :float
      add :status_id, :string
    end
    create index("survey_response", [:created_by_id], name: "survey_response_created_by_id_index")
    create index("survey_response", [:onboarding_answer_id], name: "survey_response_onboarding_answer_id_index")
    create index("survey_response", [:status_id], name: "survey_response_status_id_index")
  end
end

# schema module
defmodule MyApp.User do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:_id, :string, autogenerate: false}
  @foreign_key_type :string

  schema "user" do
    field :created_date, :utc_datetime_usec
    field :modified_date, :utc_datetime_usec
    belongs_to :created_by, MyApp.User, foreign_key: :created_by_id, references: :_id, type: :string
    field :slug, :string
    field :email, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:created_date, :modified_date, :created_by_id, :slug, :email]) |> validate_required([:_id])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateUser do
  use Ecto.Migration

  def change do
    create table("user", primary_key: false) do
      add :_id, :string, primary_key: true
      add :created_date, :utc_datetime_usec
      add :modified_date, :utc_datetime_usec
      add :created_by_id, :string
      add :slug, :string
      add :email, :string
    end
    create index("user", [:created_by_id], name: "user_created_by_id_index")
  end
end

# schema module
defmodule MyApp.StatusType do
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:db_value, :string, autogenerate: false}
  @foreign_key_type :string

  schema "status_type" do
    field :display, :string
    field :color, :string
  end

  def changeset(rec, attrs) do
    rec |> cast(attrs, [:display, :color]) |> validate_required([:db_value])
  end
end

# migration
defmodule MyApp.Repo.Migrations.CreateStatusType do
  use Ecto.Migration

  def change do
    create table("status_type", primary_key: false) do
      add :db_value, :string, primary_key: true
      add :display, :string
      add :color, :string
    end
  end
end
--- diagnostics ---
model_synthesized_user_type /user_types/user
