CREATE SCHEMA IF NOT EXISTS "custom";

CREATE TABLE "custom"."Node" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "Children" text[],
  "Parent" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."User" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "email" text,
  PRIMARY KEY ("_id")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom"."Node"."Created By" -> "custom"."User"."_id"
-- "custom"."Node"."Parent" -> "custom"."Node"."_id"
-- "custom"."User"."Created By" -> "custom"."User"."_id"

COMMENT ON COLUMN "custom"."Node"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."Node"."Parent" IS E'References "custom"."Node"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."User"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
--- diagnostics ---
model_synthesized_user_type /user_types/user
