CREATE SCHEMA IF NOT EXISTS "custom";
CREATE SCHEMA IF NOT EXISTS "option";

CREATE TABLE "custom"."Order" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "Buyer" text,
  "Items" text[],
  "Kind" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."User" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "email" text,
  "Name" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "option"."Kind" (
  "db_value" text,
  "Display" text,
  "Code" double precision,
  PRIMARY KEY ("db_value")
);

-- References without a foreign key (Bubble does not enforce referential integrity):
-- "custom"."Order"."Created By" -> "custom"."User"."_id"
-- "custom"."Order"."Buyer" -> "custom"."User"."_id"
-- "custom"."Order"."Kind" -> "option"."Kind"."db_value"
-- "custom"."User"."Created By" -> "custom"."User"."_id"

COMMENT ON COLUMN "custom"."Order"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."Order"."Buyer" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."Order"."Kind" IS E'References "option"."Kind"."db_value" (no foreign key: Bubble does not enforce referential integrity)';
COMMENT ON COLUMN "custom"."User"."Created By" IS E'References "custom"."User"."_id" (no foreign key: Bubble does not enforce referential integrity)';
--- diagnostics ---
db_reference_to_omitted /user_types/order/%f3/items_list_custom_item/%v
