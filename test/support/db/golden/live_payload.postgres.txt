CREATE SCHEMA IF NOT EXISTS "custom";
CREATE SCHEMA IF NOT EXISTS "option";

CREATE TABLE "custom"."Order" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "Buyer" text,
  "Items" text[],
  "Kind" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "custom"."User" (
  "_id" text,
  "Created Date" timestamptz,
  "Modified Date" timestamptz,
  "Created By" text,
  "Slug" text,
  "email" text,
  "Name" text,
  PRIMARY KEY ("_id")
);

CREATE TABLE "option"."Kind" (
  "db_value" text,
  "Display" text,
  "Code" double precision,
  PRIMARY KEY ("db_value")
);

ALTER TABLE "custom"."Order" ADD FOREIGN KEY ("Buyer") REFERENCES "custom"."User" ("_id");
ALTER TABLE "custom"."Order" ADD FOREIGN KEY ("Kind") REFERENCES "option"."Kind" ("db_value");
--- diagnostics ---
db_reference_to_omitted /user_types/order/%f3/items_list_custom_item/%v
