// convex/schema.ts
// Generated by BubbleEx.
// NOTE: :reference columns -> v.string() (Bubble text ids, not Convex document ids; re-key after data migration).
// NOTE: :enum columns -> v.string() (option-set member values are not rendered).
// NOTE: :api group tables are omitted.
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  orderSListX: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    customerDROPTABLEXNextLSPSNELVTFFQ: v.string(), // reference -> re-key to v.id(...)
    statusX: v.string(), // enum -> option set (member values not rendered)
    note: v.string(),
    target: v.string(), // reference -> re-key to v.id(...)
  }),

  targetLineQEnd: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
  }),

  user: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    createdDate: v.float64(),
    modifiedDate: v.float64(),
    createdBy: v.string(), // reference -> re-key to v.id(...)
    slug: v.string(),
    email: v.string(),
    email2: v.string(),
  }),

  statusQuoted: defineTable({
    bubbleId: v.string(), // primary key (Bubble _id, text)
    display: v.string(),
  }),
});
--- diagnostics ---
model_option_key_missing /option_sets/status/values/open
db_name_suffixed /user_types/user/fields/email_text
