defmodule <%= @module %>.Repo.Migrations.AddObanJobsTable do
  use Ecto.Migration

  def up, do: Oban.Migration.up(version: <%= @oban_migration %>)

  # Removes Oban's tables; its jobs are lost.
  def down, do: Oban.Migration.down(version: 1)
end
